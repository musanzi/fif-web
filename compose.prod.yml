services:
  web:
    container_name: fif-web
    environment:
      - PORT=4100
    build:
      context: .
      target: production
    restart: unless-stopped
    command: pnpm start:prod --host=0.0.0
    ports:
      - '4200:4200'
